{
  "name": "audit-service",
  "sourceRoot": "src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nx/esbuild:esbuild",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/apps-services/audit-service",
        "main": "apps-services/audit-service/src/main.ts",
        "tsConfig": "apps-services/audit-service/tsconfig.app.json",
        "assets": [],
        "bundle": false,
        "platform": "node",
        "format": ["cjs"],
        "external": ["kafkajs"]
      }
    },
    "serve": {
      "executor": "@nx/node:execute",
      "options": {
        "buildTarget": "audit-service:build"
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps-services/audit-service/jest.config.ts",
        "passWithNoTests": true
      }
    },
    "lint": {
      "executor": "@nx/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps-services/audit-service/src/**/*.ts"]
      }
    }
  },
  "tags": ["service", "api"]
}
