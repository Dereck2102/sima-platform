import './globals.css';

export const metadata = {
  title: 'SIMA Platform - QA Testing Dashboard',
  description: 'Enterprise QA testing and monitoring dashboard for SIMA Platform',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <head>
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <meta name="theme-color" content="#0f172a" />
      </head>
      <body>
        {children}
      </body>
    </html>
  );
}
