name: CI Frontend

on:
  push:
    branches: [main, develop, qa]
    paths:
      - "frontend/**"
      - ".github/workflows/ci-frontend.yml"
  pull_request:
    branches: [main, develop, qa]
    paths:
      - "frontend/**"

env:
  REGISTRY: ghcr.io
  DOCKERHUB_ORG: simaplatform
  DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
  DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}

jobs:
  build:
    runs-on: ubuntu-latest
    name: Build Frontend Images
    if: github.event_name == 'push'
    permissions:
      contents: read
      packages: write

    strategy:
      matrix:
        include:
          - name: sima-portal
            path: frontend/sima-portal
            dockerfile: frontend/sima-portal/Dockerfile
          - name: analytics-mfe
            path: frontend/analytics-mfe
            dockerfile: frontend/Dockerfile.mfe
          - name: dashboard-mfe
            path: frontend/dashboard-mfe
            dockerfile: frontend/Dockerfile.mfe
          - name: settings-mfe
            path: frontend/settings-mfe
            dockerfile: frontend/Dockerfile.mfe
          - name: sima-qa
            path: frontend/sima-qa
            dockerfile: frontend/Dockerfile.next

    steps:
      - uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Log in to Container Registry
        uses: docker/login-action@v2
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Log in to Docker Hub
        if: env.DOCKERHUB_ORG != '' && env.DOCKERHUB_USERNAME != '' && env.DOCKERHUB_TOKEN != ''
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Lowercase Repo Name
        run: |
          echo "REPO_LOWER=$(echo "${{ github.repository }}" | tr '[:upper:]' '[:lower:]')" >> $GITHUB_ENV

      - name: Build and push ${{ matrix.name }}
        uses: docker/build-push-action@v4
        with:
          context: ${{ matrix.path }}
          file: ${{ matrix.dockerfile }}
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.REPO_LOWER }}/${{ matrix.name }}:${{ github.sha }}
            ${{ env.REGISTRY }}/${{ env.REPO_LOWER }}/${{ matrix.name }}:latest
            ${{ env.DOCKERHUB_ORG }}/${{ matrix.name }}:${{ github.sha }}
            ${{ env.DOCKERHUB_ORG }}/${{ matrix.name }}:latest
